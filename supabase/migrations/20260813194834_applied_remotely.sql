-- Placeholder for migration 20260813194834, which was applied directly to the remote
-- Nawa database (Supabase dashboard / MCP apply_migration) and therefore has a
-- history entry with no file in this repo.
--
-- This file exists only so local migration history matches remote and
-- `supabase db push` can run. The schema changes are ALREADY applied remotely;
-- intentionally a no-op so re-running against another environment is safe.
SELECT 1;
